/* ============================================================
   SMS STUDIO — UI
   Arquivo reservado para navegação, transições e interface.
   Ainda não é carregado pelo index.html nesta etapa estrutural,
   para preservar exatamente o comportamento atual.
   ============================================================ */
